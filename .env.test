# Local test environment overrides
# This file is used for local development and testing
# Uncomment and use these settings when running database services locally

# PostgreSQL settings for local testing
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=test
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# MongoDB settings for local testing
MONGODB_URL=mongodb://root:example@localhost:27017
MONGODB_DB=test
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=example
